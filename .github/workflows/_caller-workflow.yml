name: Deploy Docs

on:
  push:
    branches:
      - main
    paths:
      - docs/**
      - mkdocs.yml
      - pyproject.toml
      - package.json
      - package-lock.json


jobs:
  docs:
    uses: ./.github/workflows/_deploy-docs.yml
    with:
      runner: MCRNB89090
